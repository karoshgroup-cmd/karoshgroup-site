import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karoshgroup.app',
  appName: 'Karosh Group',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
